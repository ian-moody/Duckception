{
  "version": "0.1.0",
  "scripts": {
    "serve": "parcel web/index.html",
    "web": "rm -rf dist && parcel watch web/index.html web/404.html --public-url /web",
    "server": "cargo run"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.30.0"
  }
}
