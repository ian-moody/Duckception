{
  "version": "0.1.0",
  "scripts": {
    "web": "rm -rf dist && parcel watch web/index.html web/404.html --public-url /web",
    "server": "cargo run",
    "build": "rm -rf dist && parcel build web/index.html web/404.html --public-url /web && cargo build --release --features include_site && strip ./target/release/duckception"
  },
  "dependencies": {},
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}